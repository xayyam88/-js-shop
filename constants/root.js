const ROOT_PRODUCTS = document.getElementById('products');
const ROOT_HEADER = document.getElementById('shopping-count');
const ROOT_TOP_HEADER = document.getElementById('top-header');
const ROOT_BOTTOM_HEADER = document.getElementById('bottom-header');
const ROOT_SHOPPING = document.getElementById('shopping');
const ROOT_SIDEBAR = document.getElementById('sidebar');
const ROOT_SPINNER = document.getElementById('spinner');
const ROOT_SLIDER = document.getElementById('sliderContent');
const ROOT_FOOTER = document.getElementById('footer');
const ROOT_ERROR = document.getElementById('error');